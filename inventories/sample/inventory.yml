all:
  hosts:
  children:
    logging:
      hosts:
    win:
      hosts:
        AD_master:
    linux:
      hosts:
    fixedIp:
      hosts:
    docker:
      hosts:
        docker_dmz:
        docker_service:
    firewall:
      children:
        wan_firewall:
        lan_firewall:
          hosts:
      hosts:
    AD:
      hosts:
      children:
        domainController:
          children:
            domainMaster:
              hosts:
                AD_master:
            domainReplica:
              hosts:
                # TODO replication
        CA:
          hosts:
            # ad_pki:
          children:
    rootCert:
      hosts:
        # TODO share certificate to machine not in the AD